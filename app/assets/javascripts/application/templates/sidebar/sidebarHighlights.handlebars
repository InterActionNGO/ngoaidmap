{{#if this.projects_count}}
<div class="mod-sidebar-projects mod-info">
  <h3>Projects</h3>
  <span>{{this.projects_count}}</span>
</div>
{{/if}}
{{#if this.organizations_count}}
<div class="mod-sidebar-organizations mod-info">
  <h3>Organizations</h3>
  <span>{{this.organizations_count}}</span>
</div>
{{/if}}
{{#if this.countries_count}}
<div class="mod-sidebar-countries mod-info">
  <h3>Countries</h3>
  <span>{{this.countries_count}}</span>
</div>
{{/if}}
