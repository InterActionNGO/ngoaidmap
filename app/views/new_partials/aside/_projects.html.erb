<div class="projects mod-info">
  <h3>Projects</h3>
  <% if @projects %>
    <span><%= number_with_delimiter(@projects.size) %></span>
  <%# else %>
    <!-- <span><%#= number_with_delimiter(@site.total_projects) %></span> -->
  <% end %>
</div>
